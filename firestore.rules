rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios autenticados pueden leer/escribir productos.
    match /products/{productId} {
      allow read, write: if request.auth != null;
    }
  }
}
